# ====================== 基础应用配置 ======================
APP_ENV=dev
APP_DEBUG=1
LOG_LEVEL=DEBUG

# 数据库（按你实际情况填）
DB_URL=postgresql+asyncpg://user:password@127.0.0.1:5432/yourdb

# JWT
JWT_SECRET_KEY=YOUR_SECRET_KEY
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# CORS
CORS_ORIGINS=*

DEFAULT_LLM=openai:gpt-4o-mini

# ====================== LLM 默认路由配置 ======================
# 会话默认使用哪个模型（llm_registry 里的 alias）
DEFAULT_LLM_ALIAS=gpt-4o-mini

# RAG / 向量检索默认用哪个模型做 embedding
EMBEDDING_LLM_ALIAS=gpt-4o-mini


# ====================== OpenAI ======================
# 如果用官方：
#   OPENAI_BASE_URL=https://api.openai.com/v1
# 如果用自建网关/代理：
#   OPENAI_BASE_URL=https://your-gateway-domain.com/v1
OPENAI_API_KEY=sk-openai-xxxx
OPENAI_BASE_URL=https://api.openai.com/v1

# 对话模型映射到 llm_registry 里的 alias
OPENAI_GPT4O_MINI_MODEL=gpt-4o-mini
OPENAI_GPT4O_MODEL=gpt-4o

# 向量模型（RAG）
OPENAI_EMBEDDING_MODEL=text-embedding-3-small


# ====================== Claude / Anthropic ======================
# 官方：
#   ANTHROPIC_BASE_URL=https://api.anthropic.com
# 网关：
#   ANTHROPIC_BASE_URL=https://your-gateway-domain.com/anthropic
ANTHROPIC_API_KEY=sk-anthropic-xxxx
ANTHROPIC_BASE_URL=https://api.anthropic.com

# 对应 llm_registry 中 "claude-sonnet"
CLAUDE_SONNET_MODEL=claude-3-5-sonnet-latest


# ====================== Gemini ======================
# 官方：
#   GEMINI_BASE_URL=https://generativelanguage.googleapis.com
# 网关：
#   GEMINI_BASE_URL=https://your-gateway-domain.com/google
GEMINI_API_KEY=sk-gemini-xxxx
GEMINI_BASE_URL=https://generativelanguage.googleapis.com

# 对话模型
GEMINI_FLASH_MODEL=gemini-1.5-flash

# 向量模型
GEMINI_EMBEDDING_MODEL=text-embedding-004


# ====================== DeepSeek ======================
# 官方：
#   DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# 网关：
#   DEEPSEEK_BASE_URL=https://your-gateway-domain.com/deepseek
DEEPSEEK_API_KEY=sk-deepseek-xxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 对话模型（llm_registry 中 "deepseek-chat" / "deepseek-reasoner"）
DEEPSEEK_CHAT_MODEL=deepseek-chat
DEEPSEEK_REASONER_MODEL=deepseek-reasoner


# ====================== Qwen / DashScope 兼容 ======================
# 官方 DashScope 兼容模式：
#   QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# 网关：
#   QWEN_BASE_URL=https://your-gateway-domain.com/qwen
QWEN_API_KEY=sk-qwen-xxxx
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# 对话模型
QWEN_PLUS_MODEL=qwen-plus

# 向量模型
QWEN_EMBEDDING_MODEL=text-embedding-v2


# ====================== 本地 Ollama ======================
# 一般本地默认即可，如有 Docker 端口映射按需修改
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_LLAMA3_MODEL=llama3


# ====================== 向量库 / ES / 文件存储 ======================
# ES 基本配置（后面 RAG / 混合检索会用到）
ES_HOST=127.0.0.1
ES_PORT=9200
ES_SCHEME=http
ES_INDEX_PREFIX=vs_

# 向量存储类型（先占位，后续 RAG 模块实现时用）
# 可选：faiss / milvus / es / opensearch ...
VECTOR_STORE_TYPE=faiss

# 文件存储根路径（上传文档/图片/音频等）
FILE_STORAGE_ROOT=./data/files


# ====================== HTTP 代理（如果需要） ======================
# 如果你本机需要走代理访问外网，在这里配置；如果不用，留空即可
HTTP_PROXY=
HTTPS_PROXY=
NO_PROXY=127.0.0.1,localhost
